import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["mekovancilar.pockethost.io","images.pexels.com"], // Dış alan adını buraya ekleyin
  },
};

export default nextConfig;
